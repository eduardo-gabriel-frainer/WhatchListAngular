{
  "name": "whatchlistangularfinal",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-frontend": "ng serve --open",
    "start-backend": "json-server --watch db.json --port 8080"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "json-server": "^1.0.0-beta.3"
  }
}
